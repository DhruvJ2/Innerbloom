# Core audio processing (REQUIRED)
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# Beautiful terminal output (REQUIRED)
termcolor>=2.3.0
pyfiglet>=0.8.0

# Optional: Speech recognition for lyrics extraction
# Uncomment if you want to use audio-to-text lyrics detection
SpeechRecognition>=3.10.0
pydub>=0.25.1  # For audio format conversion

# Optional: Lyrics API
# Uncomment if you want to fetch lyrics from Genius API
lyricsgenius>=3.0.0

# Optional: Better audio resampling
# Uncomment for highest quality resampling (recommended)
soxr>=0.3.0

# Development tools (optional)
pytest>=7.4.0
black>=23.0.0
mypy>=1.5.0